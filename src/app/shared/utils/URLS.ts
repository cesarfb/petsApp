import { environment } from '../../../environments/environment';

export const APP_ID       = environment.APP_ID

export const POST_URL = `${environment.API_URL}post/`
export const TAG_URL  = `${environment.API_URL}tag/`
export const USER_URL = `${environment.API_URL}user/`